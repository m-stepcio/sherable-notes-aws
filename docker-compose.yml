version: '3.8'

services:

  dynamodb:
    image: amazon/dynamodb-local
    ports:
      - "8000:8000"

  localstack:
    image: localstack/localstack
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3